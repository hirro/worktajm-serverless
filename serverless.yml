org: hirro
app: worktajm-serverless
name: worktajm-serverless
component: website

provider:
  name: aws
  stage: dev
  region: eu-north-1

inputs:
  src:
    src: ./src
    hook: npm run build
    dist: ./dist
